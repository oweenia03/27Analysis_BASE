# RAG (Retrieval-Augmented Generation) 논문 정리

**Lewis et al., 2020 (Facebook AI Research / UCL / NYU)** — "Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks"

## 1. 문제의식 (Motivation)

- 대형 사전학습 언어모델(BART, T5 등)은 파라미터 안에 지식을 암묵적으로 저장하지만, **지식을 수정·확장하기 어렵고**, **출처를 제시할 수 없으며(non-interpretable)**, **환각(hallucination)** 이 발생하기 쉬움
- 반면 검색 기반(retrieval-based) 방법은 외부 지식에 접근 가능하지만 생성 능력이 약함
- → 두 접근을 결합한 **파라메트릭(parametric) + 논파라메트릭(non-parametric) 하이브리드 메모리** 모델 제안

## 2. 모델 구조

RAG는 두 개의 컴포넌트로 구성됨:

- **Retriever**: DPR(Dense Passage Retriever) 기반
  - 질의(query)와 위키피디아 문서를 각각 bi-encoder로 임베딩
  - MIPS(Maximum Inner Product Search) + FAISS 인덱스로 top-k 문서 검색
- **Generator**: BART-large (seq2seq 모델)
  - 검색된 문서들을 조건(condition)으로 삼아 답변 생성

$$p(y|x) \approx \sum_{z \in top-k} p_\eta(z|x)\, p_\theta(y|x,z)$$

- 입력 $x$ → 문서 $z$ 검색 → $x$와 $z$를 함께 생성기에 입력 → 출력 $y$

## 3. 두 가지 변형

| 구분 | 설명 |
|---|---|
| **RAG-Sequence** | 하나의 검색 문서를 전체 생성 시퀀스 내내 고정해서 사용 (top-k 문서별로 생성 후 marginalize) |
| **RAG-Token** | 토큰마다 다른 문서를 참조 가능 (더 유연, 세밀한 결합) |

## 4. 학습 방법

- Retriever와 Generator를 **end-to-end로 공동 미세조정(fine-tuning)**
- 단, 문서 인덱스(document encoder)는 고정 — 재인덱싱 비용이 크기 때문
- Query encoder와 generator(BART)만 학습

## 5. 실험 및 결과

- **Open-domain QA** (Natural Questions, TriviaQA, WebQuestions, CuratedTrec): 기존 extractive/closed-book 모델 대비 SOTA 달성
- **Abstractive QA (MSMARCO)**: 검색 문서 없이도 자연스러운 답변 생성 가능
- **Jeopardy Question Generation**: 사실적으로 더 구체적이고 다양한 생성 결과
- **FEVER (fact verification)**: 검증 파이프라인 없이도 경쟁력 있는 성능

## 6. 주요 장점

- **지식 업데이트 용이**: 모델 재학습 없이 문서 인덱스만 교체하면 최신 지식 반영 가능
- **출처 제공(provenance)**: 어떤 문서를 참조했는지 확인 가능 → 해석 가능성↑
- **환각 감소**: 파라메트릭 지식에만 의존하지 않아 사실 오류 감소

## 7. 한계

- 검색 품질에 결과가 크게 의존 (retriever가 관련 없는 문서를 가져오면 생성 품질 저하)
- top-k 문서 수에 따른 계산 비용 증가
- 긴 문서나 복잡한 다단계 추론(multi-hop reasoning)에는 한계

---
# LoRA (Low-Rank Adaptation of Large Language Models) 논문 정리

**Hu et al., 2021 (Microsoft)** — "LoRA: Low-Rank Adaptation of Large Language Models"

## 1. 문제의식 (Motivation)

- GPT-3 같은 초대형 모델을 downstream task마다 **풀 파인튜닝(full fine-tuning)** 하면 모델 전체 파라미터를 저장·배포해야 함 → 저장/배포 비용 막대
- 기존 경량화 대안들의 한계:
  - **Adapter 계열**: 추가 레이어 삽입 → 추론 지연(inference latency) 발생
  - **Prefix-tuning 계열**: 최적화가 어렵고, prompt 길이만큼 시퀀스 길이 손실
- → **추론 지연 없이, 적은 파라미터만으로 파인튜닝**할 방법 필요

## 2. 핵심 아이디어

- 사전학습된 가중치 행렬 $W_0$는 그대로 고정(freeze)
- 업데이트($\Delta W$)를 **저랭크(low-rank) 행렬의 곱**으로 근사:

$$W_0 + \Delta W = W_0 + BA$$

- $B \in \mathbb{R}^{d \times r}$, $A \in \mathbb{R}^{r \times k}$, rank $r \ll \min(d,k)$
- 근거: 파인튜닝 시 가중치 업데이트가 **낮은 "intrinsic rank"** 를 가진다는 가설 (Aghajanyan et al. 관찰 기반)

## 3. 구조 및 학습

- $A$는 랜덤 가우시안으로 초기화, $B$는 0으로 초기화 → 학습 시작 시 $\Delta W = 0$ (기존 모델과 동일하게 출발)
- 순전파: $h = W_0 x + BAx$
- $W_0$는 gradient 업데이트 없이 고정, **$A$, $B$만 학습**
- 보통 Transformer의 **attention 가중치 행렬**($W_q$, $W_v$ 등)에만 적용 (FFN은 고정 유지해도 충분한 성능)

## 4. 주요 장점

| 항목 | 설명 |
|---|---|
| **파라미터 효율** | 학습 가능 파라미터 최대 10,000배 감소, GPU 메모리 요구량 최대 3배 감소 |
| **추론 지연 없음** | 배포 시 $W = W_0 + BA$를 미리 합쳐(merge) 두면 원래 모델과 구조·속도 동일 |
| **task 전환 용이** | task별로 $BA$만 교체하면 됨 → 같은 base 모델로 여러 task 빠르게 스위칭 |
| **낮은 저장 비용** | task별 체크포인트가 $A$, $B$만큼만 필요 (MB 단위) |

## 5. 실험 결과

- RoBERTa, DeBERTa, GPT-2, **GPT-3 175B**에 적용
- GLUE 벤치마크 등에서 풀 파인튜닝과 **동등하거나 더 나은 성능**을 훨씬 적은 파라미터로 달성
- GPT-3에서는 학습 파라미터를 **10,000배 이상 줄이면서도** 성능 유지·향상

## 6. 한계

- rank $r$, 적용할 가중치 행렬 선택 등 **하이퍼파라미터 튜닝 필요**
- 여러 task의 $\Delta W$를 하나의 배치에서 동시에 서빙하기는 어려움 (배치별로 다른 adapter를 합칠 수 없음)
- 매우 복잡한 task에서는 낮은 rank가 표현력 한계로 작용할 가능성


