# Segment Anything (SAM) 

**Kirillov et al., 2023 (Meta AI Research, FAIR)** — "Segment Anything"

## 1. Motivation

- NLP의 foundation model(대형 언어모델)이 **prompt engineering**을 통해 강력한 zero-shot/few-shot 일반화를 보이는 것에 영감을 받음
- 컴퓨터 비전에서도 CLIP, ALIGN 같은 vision-language foundation model이 등장했지만, 세그멘테이션처럼 **웹 스케일 데이터가 존재하지 않는 태스크**는 이런 흐름에서 소외되어 있었음
- → 이미지 세그멘테이션을 위한 **foundation model**을 구축하는 것이 목표
- 이를 위해 세 가지 질문에 답해야 함: (1) 어떤 **task**가 zero-shot 일반화를 가능하게 하는가? (2) 그에 맞는 **model** 구조는? (3) 이를 학습시킬 **data**는 어떻게 확보하는가?

## 2. Task: Promptable Segmentation

- NLP의 prompt 개념을 세그멘테이션에 적용: prompt는 점(foreground/background), 박스, 마스크, 자유형 텍스트 등 **"무엇을 세그먼트할지"를 나타내는 모든 정보**
- **Promptable segmentation task**: 어떤 prompt가 주어지든 **valid한 세그멘테이션 마스크**를 반환하는 것
- "valid"의 의미: prompt가 모호해서 여러 객체를 가리킬 수 있어도(예: 셔츠 위의 점이 셔츠인지 그 사람인지), 그중 **적어도 하나에 대해서는 합리적인 마스크**를 출력해야 함
- 이 task는 (1) 자연스러운 pre-training 알고리즘과 (2) prompt engineering을 통한 downstream task로의 zero-shot transfer, 둘 다를 가능하게 함
- 기존 multi-task segmentation 시스템(고정된 태스크 세트를 학습·평가)과 달리, SAM은 **훈련 시 보지 못한 새로운 task**도 프롬프트 조합을 통해 추론 시점에 수행 가능 (task generalization)

## 3. SAM (Segment Anything Model)

세 가지 컴포넌트로 구성:

| 컴포넌트 | 역할 |
|---|---|
| **Image encoder** | MAE로 사전학습된 ViT-H 기반. 이미지당 한 번만 실행(무겁지만 amortized) |
| **Prompt encoder** | sparse(점, 박스, 텍스트)와 dense(마스크) prompt를 임베딩. 텍스트는 CLIP 텍스트 인코더 사용 |
| **Mask decoder** | 이미지 임베딩 + prompt 임베딩을 받아 경량 Transformer decoder로 마스크 예측 (~50ms, 웹브라우저 CPU에서 실시간) |

- **모호성 해결**: 하나의 prompt에 대해 **3개의 마스크(whole/part/subpart)**를 동시에 예측하고, IoU 예측 헤드로 confidence score를 매김. 학습 시 최소 손실만 역전파
- **손실 함수**: focal loss + dice loss 조합
- 이미지 임베딩을 한 번 계산해두면, 다양한 prompt에 대해 재사용 가능 → 효율성 확보

## 4. Data Engine 

세그멘테이션 마스크는 웹에 풍부하지 않으므로, **모델-인-더-루프 방식의 3단계 데이터 엔진** 구축:

1. **Assisted-manual stage**: 전문 annotator가 SAM의 도움을 받아 대화형으로 마스크 라벨링 (annotation 시간 34초→14초로 단축, 4.3M masks/120k images)
2. **Semi-automatic stage**: 자동으로 confident mask를 먼저 생성하고, annotator는 나머지 덜 두드러진 객체에 집중 → 다양성 증가 (추가 5.9M masks, 총 10.2M)
3. **Fully automatic stage**: 32×32 정규 그리드 포인트로 모델에 프롬프트하여 완전 자동 생성. ambiguity-aware 모델 + confident/stable mask 선택 + NMS로 1.1B개의 고품질 마스크 생성 (99.1%가 이 단계에서 생성됨)

## 5. Dataset: SA-1B

- **11M개**의 고해상도(평균 3300×4950px), 라이선스 확보·프라이버시 보호 이미지 + **1.1B개**의 마스크
- 기존 최대 데이터셋(Open Images) 대비 **11배 많은 이미지, 400배 많은 마스크**
- 얼굴과 차량 번호판은 블러 처리
- 마스크 품질: 전문가 보정본과 IoU 비교 시 94%가 90% 이상 IoU (기존 inter-annotator consistency 85-91%보다 높음)

## 6. Responsible AI (RAI) 

- 지리적/소득 수준별 대표성 분석: SA-1B는 유럽·아시아 및 중간소득 국가 비중이 높으며, 아프리카·중남미·저소득국은 여전히 과소대표됨 (다만 모든 지역이 이전 데이터셋의 전체 마스크 수보다 10배 많은 마스크를 보유)
- 사람 세그멘테이션 성능을 perceived gender/age/skin tone별로 비교 → 대체로 그룹 간 유의미한 차이 없음 (단, 옷 세그멘테이션에서는 gender presentation에 따른 편향 발견)

## 7. Zero-Shot Transfer 실험

23개의 다양한 세그멘테이션 데이터셋으로 5가지 태스크 평가:

| 태스크 | 결과 요약 |
|---|---|
| **단일 포인트 세그멘테이션** | RITM 등 기존 interactive segmenter 대비 16/23 데이터셋에서 우수 (최대 +47 IoU), human study에서도 일관되게 높은 평점 |
| **Edge detection** (BSDS500) | 학습 없이도 HED 등 초기 딥러닝 기법보다 우수한 zero-shot 성능 |
| **Object proposal generation** (LVIS) | ViTDet-H 대비 medium/large 및 rare/common 카테고리에서 우수 |
| **Instance segmentation** | ViTDet의 박스에 SAM을 결합 → AP는 다소 낮지만, human rating에서는 SAM이 더 우수(경계가 더 깨끗함) |
| **Zero-shot text-to-mask** | CLIP 이미지/텍스트 임베딩 정렬을 활용해 텍스트만으로 세그멘테이션 가능 (proof-of-concept) |

## 8. Ablation 연구

- 데이터 엔진 3단계를 누적 학습할수록 성능 향상, 최종적으로는 **자동 생성 마스크만으로도** 전체 데이터 사용과 유사한 성능
- 전체 SA-1B의 **약 10%(1M 이미지)**만으로도 전체 데이터셋과 비슷한 성능 달성
- 이미지 인코더 스케일링: ViT-B→ViT-L은 큰 향상, ViT-L→ViT-H는 미미한 향상 (스케일링 포화 조짐)

## 9. 한계 

- 미세한 구조를 놓치거나 작은 분리된 컴포넌트를 hallucination하는 경우 있음
- 많은 포인트가 주어질 경우 전용 interactive segmentation 모델에 비해 성능이 떨어짐
- 무거운 이미지 인코더 사용 시 전체 파이프라인은 실시간이 아님
- text-to-mask는 탐색적 수준이며 완전히 robust하지 않음
- semantic/panoptic segmentation을 위한 간단한 prompt 설계 방법은 불분명

---


# CLIP (Learning Transferable Visual Models From Natural Language Supervision) 

**Radford et al., 2021 (OpenAI)** — "Learning Transferable Visual Models From Natural Language Supervision"

## 1. 문제의식 (Motivation)

- 기존 컴퓨터 비전 모델은 **고정된 카테고리 집합**(ImageNet의 1000개 클래스 등)에 대해서만 학습·평가되어, 새로운 카테고리나 태스크로 확장하려면 추가 라벨링과 파인튜닝이 필요
- 반면 NLP에서는 웹 텍스트로부터 raw text를 직접 예측하도록 학습하는 방식(GPT 계열)이 task-agnostic한 아키텍처로 zero-shot 전이에서 큰 성공을 거둠
- → 이미지 분야에서도 **자연어를 감독 신호(supervision)로 사용**하여, 고정된 카테고리에 얽매이지 않는 더 넓고 일반적인 시각 개념을 학습할 수 있지 않을까?

## 2. 핵심 아이디어: 이미지-텍스트 대조 학습 (Contrastive Pre-training)

- 인터넷에서 수집한 **4억 개의 (이미지, 텍스트) 쌍**(WIT: WebImageText)으로 학습
- 별도의 이미지 인코더와 텍스트 인코더를 각각 학습시켜, **같은 배치 내에서 실제 매칭되는 이미지-텍스트 쌍의 임베딩 코사인 유사도는 최대화**, 매칭되지 않는 쌍은 최소화하는 **contrastive objective**로 학습

$$\text{maximize} \sum_{i} \text{sim}(I_i, T_i), \quad \text{minimize} \sum_{i \ne j} \text{sim}(I_i, T_j)$$

- N개의 (이미지, 텍스트) 쌍이 배치에 있으면, N×N개의 가능한 조합 중 **N개의 정답 쌍**을 맞추는 symmetric cross-entropy loss(InfoNCE 유사) 사용
- 이미지 캡션을 **정확히 예측(generate)**하려는 기존 방식보다, 어떤 텍스트가 어떤 이미지와 짝인지만 맞추는 **대조 학습이 훨씬 효율적**임을 확인 (같은 정확도를 4배 빠르게 달성)

## 3. 모델 구조

| 컴포넌트 | 옵션 |
|---|---|
| **이미지 인코더** | ResNet 계열(ResNet-50, 개선된 버전 RN50x4 등) 또는 **Vision Transformer(ViT)** 계열 |
| **텍스트 인코더** | Transformer 기반 (GPT-2와 유사한 구조) |

- 각 인코더의 출력을 **선형 projection**을 통해 동일한 차원의 공유 임베딩 공간(multi-modal embedding space)으로 매핑
- 학습 가능한 temperature 파라미터로 softmax의 스케일 조정
- 가장 성능이 좋은 모델은 ViT-L/14를 336px 고해상도로 추가 학습한 버전(ViT-L/14@336px)

## 4. Zero-Shot Transfer 방법

- 학습 후에는 **파인튜닝 없이** 새로운 분류 태스크에 바로 적용 가능
- 방법: 분류하려는 클래스들을 "A photo of a {class}."와 같은 **텍스트 프롬프트**로 변환 → 텍스트 인코더로 임베딩 → 입력 이미지 임베딩과 가장 코사인 유사도가 높은 텍스트를 선택
- 이는 word2vec 등에서 사용되던 방식과 유사하게, 텍스트 인코더가 사실상 **시각 개념을 인식하는 zero-shot classifier의 가중치를 생성**하는 하이퍼네트워크 역할을 함
- **Prompt engineering**과 **prompt ensembling**(여러 템플릿의 평균)을 통해 성능을 크게 개선 가능함을 발견 (ImageNet 기준 약 +5%p 향상)

## 5. 실험 결과

- **30개 이상의 다양한 컴퓨터 비전 데이터셋**(OCR, 행동 인식, 지리 위치, 세분화된 분류 등)에서 평가
- **ImageNet zero-shot 정확도 76.2%** — 완전 지도학습된 원조 ResNet-50과 동등한 성능을 **어떤 ImageNet 라벨도 사용하지 않고** 달성
- Zero-shot CLIP은 종종 few-shot linear probe(같은 도메인에서 적은 라벨로 학습한 분류기)보다도 경쟁력 있는 성능을 보임
- **분포 이동(distribution shift)에 대한 강건성**이 뛰어남: ImageNetV2, ImageNet-Sketch, ImageNet-A/R 등 자연 발생 분포 변화에서 기존 ImageNet 학습 모델보다 훨씬 덜 성능이 저하됨
- 모델 크기(compute)를 늘릴수록 zero-shot 성능이 **부드럽게(smoothly)** 향상되는 스케일링 법칙 확인

## 6. Linear Probe 및 Representation 품질

- CLIP의 이미지 인코더에서 추출한 feature로 linear probe(선형 분류기만 학습)를 훈련했을 때도, 기존 최고 수준의 ImageNet 사전학습 모델(예: 지도학습 ResNet, SimCLR 등)보다 더 효율적이고 우수한 representation을 보임

## 7. 한계

- 일부 세분화된 분류(자동차 모델 구분 등), 추상적/체계적 과제(객체 개수 세기), 또는 학습 데이터 분포에서 아주 벗어난(OOD) 태스크(예: MNIST 손글씨 숫자)에서는 상대적으로 약함 — MNIST에서 zero-shot 정확도가 단순 로지스틱 회귀보다 낮게 나옴
- 여전히 fine-grained하고 전문적인 태스크에서는 완전 지도학습 모델과의 격차 존재
- 학습에 사용된 웹 이미지-텍스트 데이터의 **사회적 편향**을 그대로 학습할 위험 (논문에서 별도 섹션으로 공정성·편향 분석 수행)
- 텍스트 프롬프트의 표현 방식에 성능이 민감함 (prompt engineering 필요성)

