# GAN (Generative Adversarial Nets) 논문 정리

**Goodfellow et al., 2014 (Université de Montréal)** — "Generative Adversarial Nets"

## 1. 문제의식 (Motivation)

- 기존 딥러닝의 성공은 대부분 **판별(discriminative) 모델**(분류 등)에 집중됨
- **생성(generative) 모델**은 다루기 힘든 확률 계산(intractable probabilistic computation)이나 MCMC 근사 등으로 인해 학습이 어려움
- → 복잡한 근사 계산 없이 생성 모델을 학습할 수 있는 새로운 프레임워크 제안

## 2. 핵심 아이디어: 적대적 학습 (Adversarial Framework)

두 신경망이 **경쟁(minimax game)** 하며 동시에 학습됨:

- **Generator ($G$)**: 랜덤 노이즈 $z$를 받아 진짜 같은 가짜 데이터를 생성 — "위조지폐범" 역할
- **Discriminator ($D$)**: 입력이 진짜 데이터인지 $G$가 만든 가짜인지 판별 — "경찰" 역할

$$\min_G \max_D V(D,G) = \mathbb{E}_{x\sim p_{data}}[\log D(x)] + \mathbb{E}_{z\sim p_z}[\log(1-D(G(z)))]$$

- $D$는 진짜를 진짜로, 가짜를 가짜로 판별하는 능력을 최대화
- $G$는 $D$를 속이도록(즉 $D(G(z))$가 1에 가깝도록) 학습
- 두 네트워크가 서로를 이기려 경쟁하는 과정에서 **$G$가 실제 데이터 분포 $p_{data}$를 근사**하게 됨

## 3. 학습 방법

- $D$와 $G$를 번갈아가며 경사하강법(SGD)으로 학습
  - $D$를 $k$ 스텝 업데이트 → $G$를 1 스텝 업데이트 (논문 실험에서는 $k=1$)
- 실제 구현에서는 초기 학습 불안정을 막기 위해 $G$가 $\log(1-D(G(z)))$를 최소화하는 대신 **$\log D(G(z))$를 최대화**하도록 손실을 변형 (같은 고정점을 가지지만 초기 그래디언트가 더 강함)

## 4. 이론적 결과

- global optimum에서 $p_g = p_{data}$ (생성 분포가 실제 데이터 분포와 일치)에 도달함을 증명
- 이때 $D(x) = 1/2$ (판별자가 진짜/가짜를 구별할 수 없는 상태)
- 적절한 조건 하에서 이 minimax game의 학습 알고리즘이 수렴함을 이론적으로 보임

## 5. 구조적 특징

- $G$, $D$ 모두 다층 퍼셉트론(MLP)으로 구현 가능 → 역전파(backpropagation)만으로 학습, Markov chain이나 근사 추론 불필요
- Deep Boltzmann Machine 등 기존 생성모델과 달리 **inference 과정이 필요 없음**
- 샘플링이 단순함 (forward pass 한 번으로 생성)

## 6. 실험

- MNIST, TFD(Toronto Face Database), CIFAR-10에서 이미지 생성 실험
- 다른 생성모델과 정량 비교 어려움(likelihood 추정이 부정확)이 있었지만, 정성적으로 샘플 품질 우수함을 시연
- latent space $z$의 보간(interpolation)을 통해 생성 이미지가 단순 암기가 아닌 의미 있는 매니폴드를 학습했음을 확인

## 7. 한계

- 학습이 불안정함 (mode collapse, $D$/$G$ 균형 붕괴 등) — 이후 연구(DCGAN, WGAN 등)에서 주로 다뤄짐
- $p_g(x)$를 명시적으로 표현하지 않음 (implicit density model)
- $D$와 $G$의 학습 속도 동기화(synchronization)가 잘 안 되면 학습이 깨지기 쉬움

----
# DDPM (Denoising Diffusion Probabilistic Models) 논문 정리

**Ho, Jain, Abbeel, 2020 (UC Berkeley)** — "Denoising Diffusion Probabilistic Models"

## 1. 문제의식 (Motivation)

- Diffusion probabilistic model 자체는 이전에도 제안된 바 있었지만(Sohl-Dickstein et al., 2015), 고품질 이미지 생성에서 GAN 등에 밀려 큰 주목을 받지 못함
- 본 논문은 diffusion model이 **GAN에 필적하는(혹은 능가하는) 고품질 이미지 샘플**을 생성할 수 있음을 보이고, 이를 **denoising score matching**과 연결지어 안정적인 학습 방법을 제시

## 2. 핵심 아이디어: 두 개의 마르코프 체인

### Forward process (diffusion, 고정됨 — 학습 X)
- 실제 이미지 $x_0$에 $T$ 스텝에 걸쳐 점진적으로 가우시안 노이즈를 추가
$$q(x_t|x_{t-1}) = \mathcal{N}(x_t; \sqrt{1-\beta_t}\,x_{t-1}, \beta_t I)$$
- $T$가 충분히 크면 $x_T$는 거의 순수한 가우시안 노이즈가 됨
- **재매개변수화(reparameterization)** 덕분에 임의의 $t$에서 $x_t$를 $x_0$로부터 한 번에 샘플링 가능

### Reverse process (denoising, 학습 대상)
- 노이즈 $x_T$에서 시작해 점진적으로 노이즈를 제거하며 $x_0$를 복원
$$p_\theta(x_{t-1}|x_t) = \mathcal{N}(x_{t-1}; \mu_\theta(x_t,t), \Sigma_\theta(x_t,t))$$
- 신경망(U-Net)이 각 스텝에서 노이즈를 예측/제거하는 역할

## 3. 학습 목표의 단순화

- 원래는 variational lower bound(ELBO)를 최적화해야 하지만, 저자들은 이를 단순화한 손실 함수를 제안:

$$L_{simple} = \mathbb{E}_{t,x_0,\epsilon}\left[\|\epsilon - \epsilon_\theta(x_t, t)\|^2\right]$$

- 즉, 모델 $\epsilon_\theta$가 **각 스텝에서 추가된 노이즈 $\epsilon$ 자체를 예측**하도록 학습 (원본 이미지 $x_0$ 대신 노이즈를 예측하는 것이 학습 안정성과 샘플 품질 면에서 더 우수함을 실험적으로 발견)
- 이 단순화된 손실이 **denoising score matching**과 수학적으로 동치임을 보임 → diffusion model과 score-based generative model(Song & Ermon)을 이론적으로 연결

## 4. 네트워크 구조

- 각 타임스텝 $t$에서 노이즈를 예측하는 **U-Net** 기반 구조 사용
- 타임스텝 정보는 **sinusoidal position embedding**으로 주입 (Transformer의 positional encoding과 유사한 방식)
- self-attention 블록을 일부 해상도에 포함

## 5. 샘플링 과정

- $x_T \sim \mathcal{N}(0, I)$에서 시작
- 학습된 $\epsilon_\theta$를 이용해 $T$ 스텝(보통 1000 스텝)을 거쳐 점진적으로 노이즈 제거 → $x_0$ 생성
- **Langevin dynamics 기반 샘플링**과 유사한 형태

## 6. 실험 결과

- CIFAR-10에서 **Inception Score 9.46, FID 3.17**을 달성하며 당시 GAN 계열 대비 경쟁력 있는(혹은 더 나은) unconditional 생성 성능 입증
- LSUN(256×256) 등 고해상도 이미지에서도 고품질 샘플 생성
- 손실도 무손실(lossless) 압축 관점에서 해석 가능함을 보임 (코드 길이 관점의 분석)

## 7. 장점 및 의의

| 항목 | 설명 |
|---|---|
| **학습 안정성** | GAN과 달리 adversarial game 없이 단순 회귀(regression) 형태 손실로 학습 → mode collapse 문제 없음 |
| **이론적 명확성** | likelihood 기반 모델이면서 score matching과도 연결되는 통합적 관점 제공 |
| **샘플 다양성** | GAN 대비 mode coverage가 우수한 경향 |

## 8. 한계

- **샘플링 속도가 매우 느림** (수백~수천 스텝의 순차적 denoising 필요) — 이후 DDIM, 다양한 fast sampler 연구로 이어짐
- 학습 시 $T$, $\beta_t$ 스케줄 등 하이퍼파라미터에 성능이 민감
- 고해상도 이미지에서는 계산 비용이 크게 증가
