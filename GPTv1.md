# [논문 리뷰] Improving Language Understanding by Generative Pre-Training (GPT-1)

---

## 1. 어떤 문제를 해결하려 했는가? 
* **라벨링 데이터 부족 및 전이 한계 극복**: 대부분의 딥러닝 기반 자연어 처리 태스크는 수동으로 구축된 대규모 지도 학습 데이터셋에 의존하고 있어, 라벨이 부족한 특정 도메인에서는 모델의 일반화 성능이 급격히 저하되는 문제가 있었습니다.
* **단일 모델 기반의 범용 언어 이해 달성**: 태스크마다 아키텍처를 새로 설계하지 않고도, 대규모 비지도(Unlabeled) 코퍼스에서 언어적 지식을 먼저 학습한 뒤 소량의 라벨 데이터로 다양한 판별 태스크(NLI, QA, 문장 유사도, 텍스트 분류 등)에 효과적으로 전이(Transfer)되는 단일 범용 프레임워크를 정립하고자 했습니다.

---

## 2. 연구 동기와 문제점은 무엇인가? 
* **무한한 미라벨 텍스트의 미활용**: 현실에는 라벨링되지 않은 방대한 원시 텍스트가 존재하지만, 기존의 지도학습 패러다임은 이를 효과적으로 활용하지 못했습니다.
* **비지도 사전학습 전이의 핵심 난제**:
  * **불명확한 최적화 목적함수**: 사전학습 단계에서 언어 모델링, 오토인코더 등 어떤 목적함수가 다운스트림 태스크 전이에 가장 효과적인지 명확히 규명되지 않았습니다.
  * **비효율적인 전이 메커니즘**: 학습된 표현(Representation)을 타깃 태스크로 전달할 때 모델 구조 의존성이 높아, 각 작업마다 복잡한 커스텀 모델 구조를 덧붙여야 하는 비효율성이 존재했습니다.

---

## 3. 관련 연구 동향은 어떠한가? 
* **단어 단위 정적 임베딩 (Word2Vec, GloVe)**: 단어 하나당 고정된 단일 벡터만 할당하므로, 다의어나 문맥에 따라 변하는 동적 의미(Contextualized meaning)를 온전히 반영하지 못했습니다.
* **컨텍스트 기반 표현 및 특화 아키텍처 (ELMo)**: 양방향 LSTM을 사전학습하여 문맥 임베딩을 제공했으나, Feature-based 방식이라 타깃 태스크마다 별도의 복합 신경망 레이어를 추가로 설계해야 했습니다.
* **LSTM 기반 파인튜닝 (ULMFiT)**: 사전학습된 언어 모델 전체를 미세조정하는 방식을 제안했으나, 순환 신경망(LSTM) 특유의 순차 처리 구조로 인해 긴 문맥에 대한 장기 의존성(Long-range dependency)을 학습하는 데 구조적 한계를 보였습니다[cite: 1].

---

## 4. 연구 접근법과 모델 구조는 어떻게 되는가? 
* **2단계 프레임워크 (Two-Stage Framework)**:
  1. **비지도 생성 사전학습 (Generative Pre-training)**: 대규모 미라벨 텍스트 코퍼스(BooksCorpus)를 바탕으로 이전 토큰들을 보고 다음 토큰을 예측하는 인과적 언어 모델링(Causal LM)을 수행합니다[cite: 1].
  2. **지도 판별 미세조정 (Discriminative Fine-tuning)**: 사전학습된 가중치를 초기값으로 사용하며, 출력단에 최소한의 선형 분류 헤드($W_y$)만 결합하여 타깃 데이터셋으로 전체 파라미터를 업데이트합니다[cite: 1].
* **모델 아키텍처 (Transformer Decoder)**:
  * **12-layer Decoder-only Transformer**: 미래 토큰 참조를 방지하는 Masked Multi-Head Self-Attention 블록을 12개 적층했습니다[cite: 1].
  * **학습 가능한 위치 임베딩 (Learned Position Embeddings)**: 고정된 삼각함수 공식 대신 768차원의 파라미터 행렬을 역전파를 통해 데이터에 맞춰 최적화했습니다[cite: 1].
* **Task-specific Input Transformations (입력 평탄화)**:
  * 모델 구조의 변경을 최소화하기 위해 특수 토큰(`[Start]`, `[Delim]`, `[Extract]`)을 도입하여, 다양한 구조의 입력을 단일 1차원 시퀀스로 통일(Traversal)시켰습니다[cite: 1].

---

## 5. 실험 구성과 결과는? 
* **실험 환경**: BooksCorpus(약 7천여 권 도서, 장거리 문맥 학습 최적화), Hidden dim 768, 12 Heads, Feedforward 3072, 활성화 함수 GELU, BPE 토크나이저, 최대 시퀀스 길이 512[cite: 1].
* **주요 벤치마크 결과 (12개 태스크 중 9개 SOTA 달성)**:
  * **자연어 추론 (NLI)**: MNLI-m(82.1%), MNLI-mm(81.4%), SciTail(88.3%, +5.0%p), QNLI(88.1%, +5.8%p) 등 주요 태스크에서 기존 성능을 큰 폭으로 경신했습니다 (소규모 RTE는 56.0%)[cite: 1].
  * **질의응답 및 상식 추론**: Story Cloze(86.5%, +8.9%p), RACE(59.0%, +5.7%p)에서 기존 복합 앙상블 모델들을 단일 모델로 상회했습니다[cite: 1].
  * **문장 유사도**: STS-B(82.0), QQP(70.3, +4.2%p)에서 최고 수준의 성능을 기록했습니다[cite: 1].
  * **텍스트 분류**: 문법성 판별인 CoLA에서 45.4(+10.4p)를 달성하여 모델 내부에 축적된 언어적 편향과 문법 이해도를 입증했습니다[cite: 1].
  * **GLUE 종합 벤치마크**: 68.9점에서 72.8점으로 종합 점수를 크게 끌어올렸습니다[cite: 1].

---

## 6. 무엇을 발견했고 한계점은 무엇인가? 
* **분석을 통한 핵심 발견 (Analysis)**:
  * **전이 레이어 수의 영향**: 사전학습된 레이어를 타깃 태스크로 많이 전이할수록 성능이 꾸준히 상승하며, 12개 레이어를 전부 전이했을 때 MultiNLI 기준 최대 9%의 추가 성능 향상을 확인했습니다[cite: 1].
  * **제로샷(Zero-shot) 행동 관찰**: 별도의 지도 미세조정 없이도 사전학습 업데이트가 진행됨에 따라 휴리스틱을 통한 태스크 해결 성능이 점진적으로 향상됨을 확인했습니다[cite: 1].
  * **절제 연구 (Ablation Studies)**: 사전학습을 완전히 제거하고 처음부터 지도 학습만 진행했을 때 평균 성능이 14.8% 급락하여, 비지도 사전학습이 성능 향상의 가장 지배적인 요인임을 입증했습니다[cite: 1].
* **한계점**:
  * **단방향(Left-to-Right) 마스킹 구조**: 이전 시점의 토큰만 참조할 수 있어, 문장 전체의 양방향 문맥을 동시에 파악해야 하는 일부 NLU 과제에서 제약이 있었습니다 (이후 BERT의 등장 계기)[cite: 1].
  * **파인튜닝 의존성**: 새로운 과제를 수행할 때마다 별도의 라벨링된 데이터 수집과 가중치 업데이트 과정이 필수적이었습니다[cite: 1].
  * **모델 규모의 한계**: 1.17억 개 파라미터와 512 토큰 길이 제한으로 인해 복잡한 다단계 추론 및 초장문 처리에 한계를 보였습니다[cite: 1].

---

## 7. 결론 및 주요 요약은? 
* **단일 모델 패러다임 정립**: 생성적 사전학습과 판별적 미세조정의 결합을 통해 작업에 구애받지 않는 단일 범용(Task-agnostic) 모델의 실효성을 증명했습니다[cite: 1].
* **초거대 언어모델 혁명의 시발점**: Transformer 구조와 장거리 연속 텍스트의 결합이 비지도 학습에 최적임을 입증하였으며[cite: 1], 분석 섹션에서 관찰된 제로샷 가능성은 이후 GPT-2(Zero-shot 프롬프팅), GPT-3(Few-shot In-Context Learning), 나아가 대화형 AI(ChatGPT)로 이어지는 LLM 기술 발전의 토대가 되었습니다[cite: 1].

---

## 8. 기존 연구 대비 차별점은 무엇인가?
* **태스크 비종속적(Task-agnostic) 구조**: 다운스트림 태스크에 맞춰 모델 내부 구조를 복잡하게 변경할 필요 없이, 입력 포맷 변환(Traversal)만으로 단일 구조를 유지했습니다[cite: 1].
* **단순 임베딩 추출 탈피**: 고정된 피처를 추출해 사용하는 수준을 넘어, 사전학습된 트랜스포머의 모든 파라미터를 타깃 태스크에 맞춰 엔드투엔드로 미세조정했습니다[cite: 1].
* **Transformer 디코더 채택**: RNN 계열(LSTM)의 순차적 한계를 극복하고 셀프 어텐션을 통해 더 넓은 문맥의 장거리 의존성을 효과적으로 학습했습니다[cite: 1].

---

## 9. 핵심 아이디어는?
* **Unsupervised Pre-training $\rightarrow$ Supervised Fine-tuning**: 방대한 라벨 없는 코퍼스로 다음 단어를 맞히며 세상의 지식과 문맥을 먼저 배우고, 소량의 지도 데이터로 목표 과제에 정렬시키는 2단계 접근법입니다[cite: 1].
* **입력 평탄화(Input Traversal)**: 텍스트 쌍(문장 유사도), 전제/가설(함의), 질문/문맥/선택지(QA) 등 다채로운 형태의 입력을 `[Start]`, `[Delim]`, `[Extract]` 토큰을 활용해 직렬화하여 단일 시퀀스로 변환했습니다[cite: 1].

---

## 10. 주요 수식

### **(1) 비지도 사전학습 목적함수 (Unsupervised LM Loss)**
$$L_1(\mathcal{U}) = \sum_i \log P(u_i \mid u_{i-k}, \dots, u_{i-1}; \Theta)$$
* 이전 $k$개의 문맥 토큰이 주어졌을 때 현재 토큰 $u_i$를 예측하는 우도(Likelihood)의 로그합을 최대화합니다[cite: 1].

### **(2) 지도 미세조정 목적함수 (Supervised Fine-tuning Loss)**
$$P(y \mid x^1, \dots, x^m) = \text{softmax}(h_l^m W_y)$$
$$L_2(\mathcal{C}) = \sum_{(x, y)} \log P(y \mid x^1, \dots, x^m)$$
* 마지막 트랜스포머 블록의 최종 토큰 활성화 값($h_l^m$)에 선형 레이어($W_y$)를 곱하고 소프트맥스를 취해 라벨 $y$의 확률을 구한 후[cite: 1], 정답 로그 확률을 최대화합니다[cite: 1].

### **(3) 보조 언어 모델링이 결합된 최종 목적함수**
$$L_3(\mathcal{C}) = L_2(\mathcal{C}) + \lambda \cdot L_1(\mathcal{C})$$
* 미세조정 시 판별 손실($L_2$)에 보조 언어 모델링 손실($L_1$)을 가중치 $\lambda$로 결합하여 일반화 성능을 높이고 수렴 속도를 단축합니다[cite: 1].

---

## 11. 기타: 위치 임베딩(Position Embedding) 최적화

* **문제 제기**: 어텐션 메커니즘 내에 이미 단어 간 관계를 연산하는 내적($Q \cdot K^T$)이 존재함에도[cite: 1], 고정 삼각함수 대신 위치 벡터($W_p$)를 학습 가능한 파라미터로 최적화하는 이유는 무엇인가[cite: 1]?
* **수학적 전개 및 상호작용**:
  * 입력 벡터는 단어 임베딩 $x_i$와 위치 벡터 $p_i$의 덧셈($z_i = x_i + p_i$)으로 구성됩니다[cite: 1].
  * 쿼리와 키의 내적 시 다음과 같이 4개 항으로 전개됩니다:
    
   <img width="410" height="56" alt="image" src="https://github.com/user-attachments/assets/8ba23b4b-1e2e-469c-a4c0-adb0868a8a3f" />

    
  * 위치 정보는 독립적인 상수가 아니라, 가중치 행렬($W_Q, W_K$) 및 단어 표현과 직접 결합되어 어텐션 가중치를 동적으로 형성합니다.
* **학습 방식 채택 이유 및 트레이드오프**:
  * **표현력 및 상호 적응(Co-adaptation)**: 인위적인 삼각함수 파형에 맞추기 위해 가중치를 왜곡하는 대신, 모델 파라미터($W_Q, W_K$)와 위치 벡터($p$)가 데이터 분포에 맞게 서로 가장 적합한 잠재 공간 좌표로 함께 최적화됩니다[cite: 1].
  * **실제 성능 차이**: 벤치마크 평가에서 학습 가능한 위치 벡터가 더 우수한 정확도를 입증했습니다[cite: 1].
  * **트레이드오프**: 삼각함수 방식은 임의의 긴 시퀀스도 즉시 좌표 계산이 가능하지만, 학습 가능한 임베딩은 사전학습 시 고정한 최대 토큰 길이(GPT-1 기준 512토큰)를 넘어서는 위치는 처리할 수 없다는 한계를 가집니다[cite: 1].
